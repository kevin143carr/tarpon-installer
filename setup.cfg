[metadata]
name = tarpon_installer
version = attr: tarpon_installer_metadata.VERSION

[options]
package_dir=
    = .
packages = find:
zip_safe = False
python_requires = >= 3
[options.packages.find]
where = .
exclude =
    tests*
    .gitignore
